# ### comandi delegati a raycast ###
#
# sccp =  pbcopy  && {cursor} $(pbpaste) 
#
# ¬ = | _x && {cursor_position} $x              #!!
#
# å = | awk '{print $}'
#
# ß = | rwhile '   "$l"'
#
# ∑ = | grep -E   | awk '{print $1}'
# 
# _ = | grep -E 
# 
# † = | _
#
#
# 
# µ0, µ1, ... = prompts per llm 
# 
# chmx = chmod +x
# internet = look on the internet for details
